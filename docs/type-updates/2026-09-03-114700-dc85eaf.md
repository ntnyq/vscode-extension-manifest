# VS Code Type Update — Debugger Activation Events

- Status: success
- Completed at: 2026-09-03T11:47:00+08:00 (Asia/Shanghai)
- Scope: partial — `ExtensionActivationEvent` debugger activation forms in `src/types/union.ts`.
- Upstream repository: [microsoft/vscode](https://github.com/microsoft/vscode).
- Requested baseline: `main`, resolved once at the start of this correction.
- Current baseline: [dc85eaf99d21fb62cc4d8b43a21625a93863cf1e](https://github.com/microsoft/vscode/commit/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e).
- Previous full baseline: [2026-09-03 source audit](../vscode-source-audit.md), commit `9a9257010666f5e886b2e2b095fe9febd5a5c13c`. There were no earlier partial records.
- Source retrieval: downloaded the four unmodified source files linked below from `https://raw.githubusercontent.com/microsoft/vscode/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/` into a temporary partial snapshot. No upstream code or installation scripts were run.
- Excluded scope: all other manifest fields, contribution types, and activation events. This partial correction does not replace the previous full audit.

## Source contract

The [manifest schema](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/services/extensions/common/extensionsRegistry.ts#L261-L266) accepts activation events as strings. Its [debugger snippets](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/services/extensions/common/extensionsRegistry.ts#L282-L305) show examples rather than an exhaustive union.

The runtime's [activation dispatcher](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/contrib/debug/browser/debugAdapterManager.ts#L488-L498) emits the generic event and, when a debugger type is present, its `event:type` form. The concrete callers below establish which forms are supported. None of these activation paths requires an API proposal.

## Individual changes

### 1. ExtensionActivationEvent.onDebugAdapterProtocolTracker

- Library location: `src/types/union.ts`.
- Before: only `onDebugAdapterProtocolTracker:<type>`.
- After: both the bare event and its type-specific form, using `ExtendUnion<'onDebugAdapterProtocolTracker', true>`.
- Rationale: [debug adapter creation](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/contrib/debug/common/debugger.ts#L114-L120) passes the event and debugger type to the [shared dispatcher](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/contrib/debug/browser/debugAdapterManager.ts#L488-L498), which emits both forms.
- Compatibility: additive; existing type-specific events remain valid. No proposal requirement.
- Verification: public type regressions accept both forms, including the previously rejected bare event; built declarations were also checked.

### 2. ExtensionActivationEvent.onDebugDynamicConfigurations

- Library location: `src/types/union.ts`.
- Before: only bare `onDebugDynamicConfigurations`.
- After: also `onDebugDynamicConfigurations:<type>`, using `ExtendUnion<'onDebugDynamicConfigurations', true>`.
- Rationale: the [dynamic provider reader](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/contrib/debug/browser/debugConfigurationManager.ts#L200-L243) extracts explicit debugger types from suffixed activation events and activates those providers through the [shared dispatcher](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/contrib/debug/browser/debugAdapterManager.ts#L488-L498).
- Compatibility: additive; the generic event remains valid. No proposal requirement.
- Verification: public type regressions accept both forms and reject the misspelled `onDebugDynamicConfiguration:<type>`; built declarations were also checked.

### 3. ExtensionActivationEvent.onDebugResolve

- Library location: `src/types/union.ts`.
- Before: only `onDebugResolve:<type>`.
- After: both the bare event and its type-specific form, using `ExtendUnion<'onDebugResolve', true>`.
- Rationale: [configuration resolution](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/contrib/debug/browser/debugConfigurationManager.ts#L148-L156) calls the [shared dispatcher](https://github.com/microsoft/vscode/blob/dc85eaf99d21fb62cc4d8b43a21625a93863cf1e/src/vs/workbench/contrib/debug/browser/debugAdapterManager.ts#L488-L498), including the generic activation.
- Compatibility: additive; existing type-specific events remain valid. No proposal requirement.
- Verification: public type regressions accept both forms. An adjacent control retains the bare `onDebugInitialConfigurations` and rejects its unsupported suffixed form; built declarations were also checked.

## Related library corrections

- Both manifest writers invalidate the built-in cache after successful writes, including writes using custom serialization.
- Reads with `cache: false` or an omitted cache option bypass cache storage and do not overwrite existing cached entries. Custom maps remain caller-managed.
- `validateExtensionManifest` accepts `unknown` and returns false for null, arrays, and non-object inputs. Its boolean result remains a publisher-only check, not full manifest validation or a type predicate.
- Replaced six cache snapshot tests with behavior regressions covering both read APIs and both write APIs. Removed ten duplicate snapshots while preserving both distinct fixture payloads.
- Isolated write fixtures in temporary directories with cleanup and removed the duplicate `extensionPack` guard.

These runtime API and test corrections concern this library's behavior, not additional VS Code manifest fields.

## Verification

- Before implementation, the focused regressions failed on all four reviewed defects: stale reads after writes, cache population while disabled, null validation, and missing debugger activation forms.
- `pnpm typecheck`: passed.
- `pnpm run test`: passed all 51 tests across 5 files, with no type errors.
- `pnpm run test --update`: removed 10 redundant snapshots. An independent comparison against Git HEAD confirmed that both remaining distinct fixture payloads are unchanged.
- `pnpm run lint`: passed with only the pre-existing top-level-await warning in `scripts/check.ts`, now at line 30 after removal of the duplicate condition.
- `pnpm run format:check`: passed.
- `pnpm run build`: passed. The existing TypeScript 7 API experimental warning remains; ESM output and declarations were generated successfully.
- `git diff --check`: passed.
- Public entry checks: a temporary consumer compiled against the built export map, accepting all six generic/type-specific event forms and `unknown` validator input while rejecting the adjacent unsupported event. A built-package runtime check passed for sync/async cache bypass, write invalidation, and null validation.
- Source evidence: `verify_source_links.py` verified all 6 unique pinned source links against the downloaded partial snapshot. The relevant schema, reader, dispatcher, and callers were read to verify their meaning; the script itself checks only paths and line ranges.
- These checks ran locally on Node.js 24.19.0 with pnpm 11.25.0. GitHub's platform matrix and a live VS Code extension host were not run.
